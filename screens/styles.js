// Import StyleSheet from react-native
import { StyleSheet } from 'react-native';

// Create a stylesheet for the container with default styling for the app
export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#1F2025',
    justifyContent: 'center',
    alignItems: 'center',
  },
  defaultFont: {
    fontFamily: 'Inter',
    color: '#C2CAF2',
  },
});